name: Manual Hello Workflow

# This allows manual trigger
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Build step
        run: echo "A Demo Build"
  test:
   runs-on: ubuntu-22.04
   steps:
     - name: Test phase
       run: echo "A Demo Test"

  deploy:
   runs-on: ubuntu-22.04
   steps:
     - name: Deploy
       run: echo "A Demo Deploy"
       
    
       
